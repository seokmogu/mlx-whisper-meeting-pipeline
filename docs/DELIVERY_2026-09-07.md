# 로컬 복구 및 전달 — 2026-09-07

## 승인 범위와 완료 기준

사용자가 Notion 게시를 제외한 후속 작업과 커밋·푸시를 요청했다. 기존 대기 46건의 로컬 복구와 WDC 인덱스 신선도 개선을 진행하고, 검증된 코드·문서·합성 테스트만 GitLab/GitHub에 동일 커밋으로 전달한다.

- 대상은 2차 진단의 대기 46건으로 고정하며 새로 유입된 회의를 자동 포함하지 않는다.
- 16건은 정본 보존 후 파생본·리뷰 복구, 30건은 기존 산출물 재사용 가능성 검토 후 필요한 회의록 재생성이다.
- 원본 전사를 보존하고 변경할 회의록·파생본·리뷰와 상태의 백업을 남긴다.
- Notion 페이지·파일 업로드·권한 변경·게시 자동화 활성화는 하지 않는다.
- WDC는 소유 프로젝트의 기존 갱신 경로를 사용하고 비활성화된 LLM·스케줄러를 임의로 재개하지 않는다.
- 실제 정확도 평가는 사람 정답이 없어 미평가 상태로 유지한다.
- 복구 후 readiness 및 전사 해시를 확인하고, 전체 코드 테스트와 비공개 파일 제외를 확인한 뒤 커밋한다.
- GitLab `origin/main`, GitHub `github/main`에 같은 커밋을 푸시하고 실제 원격 SHA를 다시 읽는다.

## 실행 계획

1. WDC 소스·갱신 경로·잠금·디스크 상태를 확인하고 가능한 갱신을 수행한다.
2. 승인된 46건을 고정하고 백업한다. 검증된 기존 후보를 우선 재사용하고 필요한 재생성만 실행한다.
3. 파생본·실제 리뷰·readiness를 완성하고 원본 전사 보존 및 대기 큐의 준비 상태를 확인한다.
4. 코드·문서·합성 fixture만 스테이징해 검사·커밋·양쪽 원격 푸시를 수행한다.

## 실행 증거

비공개 복구 기록은 `state/assessments/2026-09-07-delivery/`에 저장한다.

### WDC 인덱스

- 기존 canonical DB의 약 18 GB 복구 스냅샷을 남긴 뒤 소유 프로젝트의 `wdc-index build --workers 8 --no-analysis` 경로로 재생성했다. 비활성화된 LLM 작업을 재개하지 않았다.
- 2026-09-07 10:29–10:42 KST: 파일 267,256개 검사, 추가 111,478개, 갱신 70,767개, 파생 인덱스 정리 409개, 오류 0개. WAL checkpoint, storage health, 실제 FTS 조회가 통과했다.
- 현재 인덱스는 조회 가능하지만 소스 상태는 완전하지 않다. Slack 누락 신호 372개가 남으며, 실제 자료 범위는 GitLab 2026-09, Notion·Slack 2026-08이다. 인덱스 생성 시각을 소스 신선도로 대신하지 않는다.
- 별도 소유의 current-collection·Notion 수집 작업이 실행 중인 것을 확인해 중복 실행하거나 재시작하지 않았다. 해당 작업의 후속 수집·인덱스 반영은 별도 진행 상태다.
- 근거: `../worxphere-data-collectors/.runs/wdc-index-refresh-2026-09-07/`.

### 로컬 회의록 복구

- 대기 46건을 고정했고 새로 포함된 대기 항목은 없었다. 이미 게시된 정본과의 교집합도 0건이었다.
- 기존 회의록·파생본·리뷰·팩트체크·상태 367개를 백업하고 해시 일치를 확인했다.
- 기존 후보 60개를 검사해 현재 전사와 팩트체크 해시가 맞는 1건을 재사용했다. 나머지 29건은 기존 파이프라인으로 재생성한다.
- 각 회의의 쓰기 범위는 분리하며 공통 job 상태는 단일 부모 프로세스에서 저장한다. 재생성이 필요 없는 17건의 후속 리뷰는 별도의 분리된 작업으로 진행하고 공통 큐·job 상태를 쓰지 않는다.
- 회의록 재생성 29건과 검증된 후보 재사용 1건을 완료했고, 나머지 정본 16건은 변경하지 않았다. 모든 생성 프로세스가 정상 종료했다.
- 실제 reviewer와 readiness 검증은 **46/46건 통과**했다. 독립된 리뷰 작업에서 먼저 검증한 42건을 재사용하고, 마지막 4건을 확인한 뒤 공통 job 상태를 정리했다.
- 원본 전사 **46개 SHA-256 불변**, 변경된 노트의 팩트체크 연결 **30/30 일치**, 백업 **367/367 해시 일치**를 확인했다.
- 최종 대기 큐의 오류는 **0건**, 미완료 local job은 **0건**이다. 대기 목록 `pending.txt`와 게시 추적 `publications.json`의 해시도 변경되지 않았다. 큐에 남은 46건은 로컬 준비 완료이며 게시된 상태가 아니다.
- 전체 진단의 exit 1은 이번 범위 밖의 과거 문서 73건과 정본 밖의 과거 게시 참조 1건 때문이다. 이를 현재 대기 46건의 실패로 계산하지 않는다.
- 대표 SVG 2종의 XML 및 네이티브 썸네일 생성을 확인했다. 브라우저 미리보기는 `file://` URL 보안 정책에 차단되어 수행하지 못했으며 우회하지 않았다. 브라우저·Notion 화면 검증 완료로 주장하지 않는다.
- 근거: `state/assessments/2026-09-07-delivery/final-recovery-summary.json`, `final-audit.json`, `recovery-status.json`, `backup-manifest.json`, `prereview*-status.json`.

### 코드 검증 및 Git 사전 확인

- 전체 테스트 150개 통과(29.862초), 실험 테스트 7개 통과, Python 41개·Bash 24개 문법 검사 통과.
- 독립 리뷰에서 추가 차단 결함은 발견되지 않았다.
- 스테이징 대상은 코드·문서·합성 fixture 28개이며 실제 오디오·전사·노트·상태·로그는 포함하지 않았다. 비밀값 패턴 검사와 `git diff --cached --check`가 통과했다.
- 시작 시 로컬 `HEAD`, GitLab `origin/main`, GitHub `github/main`은 모두 `2594b187e9aec25b5613eab5b53ea7b8d307894d`였다. 양쪽 push dry-run에서 인증 오류가 없었다.
- 구현은 **`32dceca231a0658183516f6f3b544e1e638c97c1`**로 커밋해 GitLab/GitHub `main`에 푸시했고 양쪽 SHA를 직접 다시 읽어 일치를 확인했다. 열린 GitLab MR과 GitHub PR은 각각 0개였다.
- 이 운영 결과는 구현 커밋 이후 별도 문서 커밋으로 전달한다. 최종 Git 상태 및 원격 SHA 확인은 사용자 응답과 로컬 `state/assessments/2026-09-07-delivery/`의 전달 증거에 남긴다.

## 남은 제한

- WDC 인덱스 갱신 자체는 완료했지만, 마지막 재조회에서도 Slack 누락 372건은 남았다. 별도 소유의 current-collection 및 Notion crawl이 실행 중이어서 종료를 기다린 뒤 소유 프로젝트 경로로 재색인해야 한다. 해당 실행을 중단하거나 중복 실행하지 않았다.
- 이번에 확인한 품질은 회의록 계약, 전사·팩트체크 연결, 파생본·리뷰·readiness 일치다. 사람 정답이 없어 실제 ASR 정확도 수치는 미평가다.
- Notion 게시·실제 업로드·게시 자동화 활성화는 수행하지 않았다. 기존 레거시 문서의 일괄 이전·삭제도 하지 않았다.

승인된 46건의 로컬 복구와 검증, 원본 보존, 구현 코드의 양쪽 원격 전달을 완료했다. 위 외부 수집 상태와 미평가 항목은 남은 제한으로 보존한다.
